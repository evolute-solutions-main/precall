# precall.evolutesolutions.io now lives inside the cold-traffic app at /precall.
# This service only forwards there (302 = revertible; nothing cached permanently).
server {
    listen $PORT;
    location / {
        return 302 https://inbound.evolutesolutions.io/precall$is_args$args;
    }
}
